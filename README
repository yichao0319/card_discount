TODO 

- Deployment:
1. change the host name in config/environments/development.rb
    config.action_mailer.default_url_options = { host: 'localhost', port: 3000 }


- Login:
1. allow to login using "account"
2. allow to login with Facebook account

